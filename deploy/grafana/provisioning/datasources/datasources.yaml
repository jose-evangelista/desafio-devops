apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Fixed uid: the dashboard JSON references it directly, and a generated uid
    # would break provisioning.
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
